__common: &common
    language: minimal
    script: ci/run.bash
    cache:
        directories:
        - target

matrix:
    include:
    -   <<: *common
        os: linux
        dist: trusty
    -   <<: *common
        os: osx
    -   <<: *common
        os: windows
